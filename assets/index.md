# 资源目录

此处存放供正文、设定、图鉴和印象板调用的统一资源。

## 子目录

- `images/`：图片、封面、地图、角色图与视觉参考。
- `paper/`：论文、参考资料与 PDF 存档。

## Markdown 调用规则

- 仓库内图片使用相对路径，不使用 GitHub `blob/main` 地址。
- 路径分隔符统一写成 `/`，例如 `../../assets/images/地图/全境地形图.png`。
- 新增图片优先按主题放入 `assets/images` 下的既有分类。